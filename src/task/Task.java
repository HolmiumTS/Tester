package task;

public class Task {
    /**
     * init a task
     *
     * @param root           root dir of the Task
     * @param compileCommand compile command, name place plz uses '%s' instead
     * @param runCommand     run command, name place uses '%s' instead
     * @param timeLimitMs    max running time for each test in ms
     */
    public Task(String root, String compileCommand, String runCommand, long timeLimitMs) {

    }

    /**
     * run a task
     *
     * @throws TaskException if something wrong
     */
    public void run() throws TaskException {

    }
}
